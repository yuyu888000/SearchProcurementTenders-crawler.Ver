# 更新說明 (Changelog)

本文件記錄 Taiwan Tender MCP 的變更細節。

## [0.0.4] - 2026-09-14

### 新功能 (Added)
- **`search_tenders_by_category`：直接用「標的分類」查詢**。走官網「標的分類查詢」
  `readTenderProctrg`，是三支工具中唯一能真的按分類代碼（52／521／522／867／8671~8676…）
  篩選的入口，不必再用標案名稱關鍵字去猜、也不必用 `get_tender_detail` 逐筆掃分類。
  支援招標／決標、標案狀態、招標方式、機關名稱包含字串（縣市近似篩選）與標案名稱排除字。
- **`cpc-catalog.ts`：執行期抓取標的分類代碼表**。官網下拉選單的 option value 是內部 pk
  （8672 → `50003003`），不是代碼本身。改為在執行期打 `/ccs/queryCPCsByType?type=E|F|3S`
  取回並快取，避免把 230 多筆對照表寫死在程式裡。

### 重要差異 (Notes)
- **這支的分頁是可用的**：`d-<id>-p` 直接帶頁碼就有下一頁，因此會自動翻頁抓到
  「官網總筆數 == 實際抓取數」，不像 `search_tender_archive` 只能拿最新 100 筆。
- 踩到的三個坑（已寫進 `proctrg-crawler.ts` 註解）：
  1. 表單標 `method="post"`，但送出函式註明「改用 get 送出」，用 POST 會拿到 HTTP 200
     的空表單頁、沒有任何錯誤訊息。
  2. **日期必須送西元**（`2026/07/01`）。畫面填民國只是顯示，送民國會被前端的
     `y1 < 2010` 判為「99 年以前」，伺服器靜默回空表單頁，極易誤判成 0 筆。
  3. 分類要送內部 pk，不是代碼。

### 已知限制 (Known Limitations)
- 未登入時公告日期區間上限 **186 天**，超過官網會直接導去全文檢索頁；本工具會擋下並請使用者拆段。
- 僅提供民國 **99 年**以後的資料（官網限制，1、2 代系統標的分類不同）。
- 官網此查詢**沒有縣市／履約地點欄位**，`orgNameIncludes` 是比對機關名稱的近似作法：
  中央機關在該縣市的案子撈不到、該縣市機關在外縣市的案子會被留下。精確履約地點需
  `get_tender_detail` 逐筆確認。
- 上層分類（如 52、867）實測回 0 筆——標案都掛在子分類上，查上層要自行展開子碼。

### 驗收 (Verification)
- `_smoke_proctrg.mjs`：12 項（代碼解析、pk 對應、186 天界線、完整翻頁、欄位解析、
  機關與排除字篩選）。實測 8672 決標公告 115/07/01~115/09/14：官網 1264 筆、抓取 1264 筆、無截斷。

## [0.0.3] - 2026-09-02

### 新功能 (Added)
- **`search_tender_archive`：可查已截止的歷史標案**。走官網「全文檢索（電子公報）」
  `readBulletion`，涵蓋民國 88 年起的公報，補上 `search_tenders`（只有等標期內）查不到的範圍。
  支援布林查詢語法、公報種類（招標／決標／公開閱覽及公開徵求／政府採購預告）、
  多年度（一次最多 3 年）與本地日期區間過濾；輸出自動分成「等標期內」與「已截止／歷史案」兩段。
  回傳的「查看」連結是 `tpam?pk=`，可直接餵給 `get_tender_detail`。
- **MCP server instructions**：把「查標案要分等標期內／已截止兩段回報、0 筆也要明寫」等
  行為規則寫進 server instructions，任何 client 掛上這支 MCP 就生效。

### 已知限制 (Known Limitations)
- 全文檢索**單次上限 100 筆／年度**：`pageSize` 帶 200/500/1000 一律只回 100 筆；
  官網分頁（`d-<id>-p`）是伺服器 session 狀態式的，GET／POST／完整 cookie jar／正確 Referer
  四種組合實測都只回沒有結果的表單頁（同一網址在瀏覽器可用）。
  因此改為「取最新 100 筆並誠實回報官網命中總數」，超出時提示縮小條件，不用瀏覽器自動化硬翻。

### 驗收 (Verification)
- `_smoke_archive.mjs`：36 項（年度解析、列表解析、截斷回報、歷史年度已截止判定、日期過濾、多年度合併）。
- `_smoke_mcp_archive.mjs`：stdio handshake 檢查工具註冊、instructions 內容與實際呼叫輸出分段；
  以「讀到回應才往下」取代定時 sleep，避免舊 `_smoke_mcp.mjs` 的假 FAIL。

## [0.0.2] - 2026-02-02

### 結構優化 (Refactoring)
- 重構專案架構，將爬蟲邏輯、業務邏輯與工具函數分離至 `services` 與 `utils` 目錄。
- 統一日期處理邏輯，建立 `src/utils/date.ts`。

### 爬蟲功能增強 (Crawler Improvements)
- **編碼修復**：引入 `iconv-lite` 自動處理政府網站的 Big5 編碼問題，修正搜尋結果亂碼。
- **解析優化**：改進標案案號與案名的拆分邏輯，提升抓取成功率。
- **Header 模擬**：優化瀏覽器 Header 模擬，降低被網站攔截的風險。

### 其他變更 (Other Changes)
- 修正 TypeScript 在 strict 模式下的型別錯誤。
- 清理根目錄，將測試腳本移至 `debug/` 資料夾。
- 更新版本號至 0.0.2 並同步 MCP Server 版本資訊。

## [0.0.1] - 2026-01-28

### 新功能 (Added)
- 初始版本：支援基本爬蟲搜尋與 MCP 工具介接。

---
*最後編輯: 2026-09-02*